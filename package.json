{
  "name": "agent-console",
  "version": "1.0.0",
  "private": true,
  "description": "A browser terminal grid for coding agents — run Claude Code, Codex, Cursor, Grok, Gemini and Pi side by side, and wire them together so they share context.",
  "license": "MIT",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "node setup.mjs",
    "check": "node check.mjs",
    "test": "node --test test/smoke.mjs"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
